{
  "name": "@varco/worker",
  "version": "0.0.0",
  "private": true,
  "description": "Job asincroni — sync catalogo, classificazione SKU, generazione documenti",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && node --watch dist/index.js",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "enqueue:demo-sync": "node --import tsx src/scripts/enqueue-demo-sync.ts"
  },
  "dependencies": {
    "@varco/database": "workspace:*",
    "@varco/shared": "workspace:*",
    "bullmq": "^5.53.2",
    "drizzle-orm": "^0.44.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "@varco/tsconfig": "workspace:*",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
